#include "main.h"

/**
  * add - a function that adds two integers and returns the result
  * @a: first integer
  * @b: second integer
  * Return: a + b
  */

int add(int a, int b)
{
	return (a + b);
}
